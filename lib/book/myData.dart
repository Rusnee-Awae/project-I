class myData {
  String name, message, profession, lastname, Date, Time , list ,note;

  myData(this.name, this.message, this.profession, this.lastname,this.Date,
      this.Time , this.list, this.note);
}